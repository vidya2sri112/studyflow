{
  "name": "study-planner-app",
  "version": "1.0.0",
  "description": "Personal study planner with pomodoro timer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "install-client": "cd client && npm install",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.18.0",
    "mongoose": "^7.5.0",
    "react-scripts": "^0.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
